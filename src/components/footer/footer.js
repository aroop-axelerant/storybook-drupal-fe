/**
 * Footer component behaviour.
 * No interactive behaviour required — stub for future enhancements.
 *
 * @param {HTMLElement} el - The root `.site-footer` element.
 */
export function initFooter(el) {}
