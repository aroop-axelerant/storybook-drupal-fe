/**
 * Table component behaviour.
 * @param {HTMLElement} el - The `.price-table` root element.
 * No interactive behaviour required — static data table.
 */
export function initTable(el) {} // eslint-disable-line no-unused-vars
