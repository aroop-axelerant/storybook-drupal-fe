/**
 * Breadcrumb component behaviour.
 * @param {HTMLElement} el - The `.breadcrumb` root element.
 * No interactive behaviour required — static navigation landmark.
 */
export function initBreadcrumb(el) {} // eslint-disable-line no-unused-vars
